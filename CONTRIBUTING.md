# Contributing to Enoki

Enoki への貢献を考えてくれてありがとうございます。
小さな誤字の修正から機能追加まで歓迎します。OSS への参加が初めてでも大丈夫です。

日本語でも英語でも構いません（メンテナの母語は日本語です）。

## Issues

次のいずれも [Issue](https://github.com/hsm-hx/enoki/issues) で受け付けています。

- **バグ報告** — 動かない・落ちる・表示がおかしい（Bug report テンプレート）
- **機能提案** — こうなったら嬉しい（Feature request テンプレート）
- **質問** — 使い方が分からない、ドキュメントが読み取れない（空の Issue で構いません）

バグ報告では、**何をしたら / 何が起きて / 何が起きてほしかったか**の 3 つが分かると助かります。
ログの出し方はテンプレートに書いてあります。

## Development

外部依存パッケージはありません。clone してすぐビルドできます。

```bash
# 1. fork して clone する
git clone https://github.com/<your-account>/enoki.git
cd enoki

# 2. ブランチを切る
git switch -c fix/bubble-position

# 3. ビルドして動かす（依存のインストール手順はありません）
make run

# 4. テストを流す
make test
```

必要なもの・動作確認済み環境は [README の Requirements](README.md#requirements) を見てください
（macOS 13 以降 / Swift 5.9 以降）。

- **Linter / formatter は導入していません。** 周りのコードのスタイル（インデント 4、
  日本語のコメント、`// MARK: -` での区切り）に合わせてください。
- ロジックは可能なら `Sources/EnokiCore/`（AppKit 非依存）に置いてください。ここはテストできます。
  AppKit に触る部分は `Sources/Enoki/` です。
- 振る舞いを変えたら `Tests/EnokiCoreTests/` にテストを足してください。
- 設計の全体像は [docs/architecture.md](docs/architecture.md) にあります。

## Pull Requests

PR には次を書いてください（`.github/pull_request_template.md` がひな型です）。

- **What** — 何を変えたか
- **Why** — なぜ必要か（Issue があれば番号を書く）
- **How to test** — レビュアーが手元で確認する手順
- **Screenshots** — 見た目が変わる場合はスクリーンショットか短い画面収録を添えてください
  （吹き出しの位置・スプライトの表示など、文章では伝わりにくいものが多いので助かります）

マージ前に `make test` が通ることを確認してください。

## Scope

Enoki は「デスクトップでキャラクターと一緒に暮らす」ための**小さなアプリ**です。
次のような変更は、**先に Issue で相談**してください（作ってから方針が合わずお蔵入り、を避けるためです）。

- ネットワーク通信を伴う機能（現在 Enoki は通信を一切しません。プライバシー方針の変更になります）
- 追加の権限（アクセシビリティ・画面収録など）を必要とする機能
- 外部パッケージへの依存追加
- 大規模なアーキテクチャ変更、全面的なリファクタリング
- macOS 以外のプラットフォーム対応

小さなバグ修正・ドキュメント改善・テスト追加は、Issue なしで直接 PR してもらって構いません。

## Assets

**キャラクターアセットはソースコードとは条件が違います。** 詳しくは
[ASSETS_LICENSE.md](ASSETS_LICENSE.md) を読んでください。

- 同梱の公式デモキャラクター **御影栞** のスプライト・キャラクターデザイン・台詞テキストは、
  **MIT License の対象外**です。自由素材ではないので、**再配布・商用利用・別作品への転用はできません。**
- **PR で画像やフォントなどの素材を追加する場合、投稿者自身がその素材を利用・再配布する権利を
  持っている必要があります。** 出所（自作 / ライセンス）を PR に書いてください。
  権利が確認できない素材はマージできません。
- 第三者のロゴ・商標・公式アセット（Codex / OpenAI 由来のものを含む）は追加しないでください。
- 個人的な設定データ（イベント日程 `renofa-schedule.json` など）はコミットしないでください
  （`.gitignore` 済みです）。

## License

PR を送ることで、あなたのコード変更が [MIT License](LICENSE) の下で配布されることに同意したものとみなします。
